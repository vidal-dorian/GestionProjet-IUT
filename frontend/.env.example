VITE_API_URL=http://localhost:8000

# Développement local uniquement : e-mail à utiliser via l'en-tête
# X-Dev-Email quand le backend tourne avec DEV_BYPASS_AUTH_ENABLED=true (pas
# de Cloudflare Access en local). Sans effet en production. Laisser vide si
# le backend est configuré avec Cloudflare Access.
VITE_DEV_EMAIL=dev@example.com
